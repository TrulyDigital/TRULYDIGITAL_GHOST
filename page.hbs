{{#post}}
<main>
    <article>
        {{#match @page.show_title_and_feature_image}}
        <header>
            <h1>{{title}}</h1>
            <p>{{custom_excerpt}}</p>
        </header>
        {{/match}}
    </article>
    <section>
        {{content}}
    </section>
</main>
{{/post}}